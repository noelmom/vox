__pycache__/
*.pyc
*.pyo
.DS_Store

# Symlinks created by setup.sh pointing into ~/Library/Application Support/Vox/
.venv
.env

# Runtime data — lives in Application Support, not the project folder
outputs/
input/
data/
voices/*
!voices/noelmo-normal.wav

# UI sketches — local design references only
sketches/

# React SPA source dependencies (build output ui-dist/ IS committed — needed for vox.sh update)
ui-src/node_modules/
ui-src/bun.lock

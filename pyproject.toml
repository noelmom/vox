[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-ra"
pythonpath = ["."]

[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  "ui-src",
  "ui-dist",
  "working-poc",
]

[tool.ruff.lint]
select = ["F"]
ignore = [
  "E501",
]

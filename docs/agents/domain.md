# Domain Docs

This is a single-context repository.

Before exploring an area, read the relevant root `CONTEXT.md` and any ADRs under `docs/adr/` when they exist. If they do not exist, proceed silently. Use the terminology established in `CONTEXT.md`, and surface any conflict with an ADR rather than silently overriding it.
